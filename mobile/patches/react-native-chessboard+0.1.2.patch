diff --git a/node_modules/react-native-chessboard/lib/commonjs/components/suggested-dots/PlaceholderDot.js b/node_modules/react-native-chessboard/lib/commonjs/components/suggested-dots/PlaceholderDot.js
index 07931d6..33a9e64 100644
--- a/node_modules/react-native-chessboard/lib/commonjs/components/suggested-dots/PlaceholderDot.js
+++ b/node_modules/react-native-chessboard/lib/commonjs/components/suggested-dots/PlaceholderDot.js
@@ -46,7 +46,7 @@ const PlaceholderDot = /*#__PURE__*/_react.default.memo(_ref => {
   const rPlaceholderStyle = (0, _reactNativeReanimated.useAnimatedStyle)(() => {
     const canBeSelected = isSelectable.value;
     return {
-      opacity: (0, _reactNativeReanimated.withTiming)(canBeSelected ? 0.15 : 0)
+      opacity: (0, _reactNativeReanimated.withTiming)(canBeSelected ? 0.62 : 0)
     };
   }, []);
   return /*#__PURE__*/_react.default.createElement(_reactNative.View, {
@@ -57,7 +57,7 @@ const PlaceholderDot = /*#__PURE__*/_react.default.memo(_ref => {
     },
     style: [styles.placeholderContainer, {
       width: pieceSize,
-      padding: pieceSize / 3.2,
+      padding: 0,
       transform: [{
         translateX: translation.x
       }, {
@@ -65,9 +65,7 @@ const PlaceholderDot = /*#__PURE__*/_react.default.memo(_ref => {
       }]
     }]
   }, /*#__PURE__*/_react.default.createElement(_reactNativeReanimated.default.View, {
-    style: [{
-      borderRadius: pieceSize
-    }, styles.placeholder, rPlaceholderStyle]
+    style: [styles.placeholder, rPlaceholderStyle]
   }));
 });
 
@@ -81,8 +79,7 @@ const styles = _reactNative.StyleSheet.create({
   },
   placeholder: {
     flex: 1,
-    backgroundColor: 'black',
-    opacity: 0.2
+    backgroundColor: '#F4EE68'
   }
 });
 //# sourceMappingURL=PlaceholderDot.js.map
\ No newline at end of file
diff --git a/node_modules/react-native-chessboard/lib/module/components/suggested-dots/PlaceholderDot.js b/node_modules/react-native-chessboard/lib/module/components/suggested-dots/PlaceholderDot.js
index 58a52d3..21698db 100644
--- a/node_modules/react-native-chessboard/lib/module/components/suggested-dots/PlaceholderDot.js
+++ b/node_modules/react-native-chessboard/lib/module/components/suggested-dots/PlaceholderDot.js
@@ -30,7 +30,7 @@ const PlaceholderDot = /*#__PURE__*/React.memo(_ref => {
   const rPlaceholderStyle = useAnimatedStyle(() => {
     const canBeSelected = isSelectable.value;
     return {
-      opacity: withTiming(canBeSelected ? 0.15 : 0)
+      opacity: withTiming(canBeSelected ? 0.62 : 0)
     };
   }, []);
   return /*#__PURE__*/React.createElement(View, {
@@ -41,7 +41,7 @@ const PlaceholderDot = /*#__PURE__*/React.memo(_ref => {
     },
     style: [styles.placeholderContainer, {
       width: pieceSize,
-      padding: pieceSize / 3.2,
+      padding: 0,
       transform: [{
         translateX: translation.x
       }, {
@@ -49,9 +49,7 @@ const PlaceholderDot = /*#__PURE__*/React.memo(_ref => {
       }]
     }]
   }, /*#__PURE__*/React.createElement(Animated.View, {
-    style: [{
-      borderRadius: pieceSize
-    }, styles.placeholder, rPlaceholderStyle]
+    style: [styles.placeholder, rPlaceholderStyle]
   }));
 });
 const styles = StyleSheet.create({
@@ -62,8 +60,7 @@ const styles = StyleSheet.create({
   },
   placeholder: {
     flex: 1,
-    backgroundColor: 'black',
-    opacity: 0.2
+    backgroundColor: '#F4EE68'
   }
 });
 export { PlaceholderDot };
diff --git a/node_modules/react-native-chessboard/lib/typescript/components/piece/index.d.ts b/node_modules/react-native-chessboard/lib/typescript/components/piece/index.d.ts
index a93964c..9b084ff 100644
--- a/node_modules/react-native-chessboard/lib/typescript/components/piece/index.d.ts
+++ b/node_modules/react-native-chessboard/lib/typescript/components/piece/index.d.ts
@@ -1,4 +1,4 @@
-import type { Move, Square } from 'chess.js';
+import type { Move, PieceType as PromotionPieceType, Square } from 'chess.js';
 import React from 'react';
 import type { PieceType, Vector } from '../../types';
 declare type PieceProps = {
@@ -8,7 +8,7 @@ declare type PieceProps = {
     size: number;
 };
 export declare type ChessPieceRef = {
-    moveTo: (square: Square) => Promise<Move | undefined>;
+    moveTo: (square: Square, promotionPiece?: PromotionPieceType) => Promise<Move | undefined>;
     enable: (activate: boolean) => void;
 };
 declare const Piece: React.MemoExoticComponent<React.ForwardRefExoticComponent<PieceProps & React.RefAttributes<ChessPieceRef>>>;
diff --git a/node_modules/react-native-chessboard/lib/typescript/context/board-refs-context/index.d.ts b/node_modules/react-native-chessboard/lib/typescript/context/board-refs-context/index.d.ts
index 601d1d5..2125573 100644
--- a/node_modules/react-native-chessboard/lib/typescript/context/board-refs-context/index.d.ts
+++ b/node_modules/react-native-chessboard/lib/typescript/context/board-refs-context/index.d.ts
@@ -1,4 +1,4 @@
-import type { Move, Square } from 'chess.js';
+import type { Move, PieceType, Square } from 'chess.js';
 import React from 'react';
 import { ChessboardState } from '../../helpers/get-chessboard-state';
 import type { ChessPieceRef } from '../../components/piece';
@@ -9,6 +9,7 @@ export declare type ChessboardRef = {
     move: (_: {
         from: Square;
         to: Square;
+        promotion?: PieceType;
     }) => Promise<Move | undefined> | undefined;
     highlight: (_: {
         square: Square;
diff --git a/node_modules/react-native-chessboard/lib/typescript/context/props-context/index.d.ts b/node_modules/react-native-chessboard/lib/typescript/context/props-context/index.d.ts
index 42e9bea..79b8b0d 100644
--- a/node_modules/react-native-chessboard/lib/typescript/context/props-context/index.d.ts
+++ b/node_modules/react-native-chessboard/lib/typescript/context/props-context/index.d.ts
@@ -14,6 +14,10 @@ declare type ChessboardColorsType = {
     lastMoveHighlight?: string;
     checkmateHighlight?: string;
     promotionPieceButton?: string;
+    selectedSquareHighlight?: string;
+    promotionBackdrop?: string;
+    promotionSurface?: string;
+    promotionBorder?: string;
 };
 declare type ChessboardDurationsType = {
     move?: number;
@@ -58,6 +62,8 @@ declare type ChessboardProps = {
      * Useful if you want to customise the default durations used in the chessboard (in milliseconds).
      */
     durations?: ChessboardDurationsType;
+    flipped?: boolean;
+    promotionLabels?: Partial<Record<'q' | 'r' | 'b' | 'n', string>>;
 };
 declare type ChessboardContextType = ChessboardProps & Required<Pick<ChessboardProps, 'gestureEnabled' | 'withLetters' | 'withNumbers' | 'boardSize'>> & {
     pieceSize: number;
diff --git a/node_modules/react-native-chessboard/src/components/piece/index.tsx b/node_modules/react-native-chessboard/src/components/piece/index.tsx
index bbbd539..59597bb 100644
--- a/node_modules/react-native-chessboard/src/components/piece/index.tsx
+++ b/node_modules/react-native-chessboard/src/components/piece/index.tsx
@@ -1,4 +1,4 @@
-import type { Move, Square } from 'chess.js';
+import type { Move, PieceType as PromotionPieceType, Square } from 'chess.js';
 import React, { useCallback, useImperativeHandle } from 'react';
 import { Gesture, GestureDetector } from 'react-native-gesture-handler';
 import Animated, {
@@ -26,7 +26,10 @@ type PieceProps = {
 };
 
 export type ChessPieceRef = {
-  moveTo: (square: Square) => Promise<Move | undefined>;
+  moveTo: (
+    square: Square,
+    promotionPiece?: PromotionPieceType
+  ) => Promise<Move | undefined>;
   enable: (activate: boolean) => void;
 };
 
@@ -43,6 +46,7 @@ const Piece = React.memo(
       const {
         durations: { move: moveDuration },
         gestureEnabled: gestureEnabledFromChessboardProps,
+        colors: { selectedSquareHighlight },
       } = useChessboardProps();
 
       const gestureEnabled = useDerivedValue(
@@ -70,14 +74,14 @@ const Piece = React.memo(
       );
 
       const wrappedOnMoveForJSThread = useCallback(
-        ({ move }: { move: Move }) => {
-          onMove(move.from, move.to);
+        ({ move, promotionPiece }: { move: Move; promotionPiece?: PromotionPieceType }) => {
+          onMove(move.from, move.to, promotionPiece);
         },
         [onMove]
       );
 
       const moveTo = useCallback(
-        (from: Square, to: Square) => {
+        (from: Square, to: Square, promotionPiece?: PromotionPieceType) => {
           return new Promise<Move | undefined>((resolve) => {
             const move = validateMove(from, to);
             const { x, y } = toTranslation(move ? move.to : from);
@@ -92,7 +96,7 @@ const Piece = React.memo(
                 offsetY.value = translateY.value;
                 isGestureActive.value = false;
                 if (move) {
-                  runOnJS(wrappedOnMoveForJSThread)({ move });
+                  runOnJS(wrappedOnMoveForJSThread)({ move, promotionPiece });
                   // Ideally I must call the resolve method
                   // inside the "wrappedOnMoveForJSThread" after
                   // the "onMove" function.
@@ -131,8 +135,8 @@ const Piece = React.memo(
         ref,
         () => {
           return {
-            moveTo: (to: Square) => {
-              return moveTo(square, to);
+            moveTo: (to: Square, promotionPiece?: PromotionPieceType) => {
+              return moveTo(square, to, promotionPiece);
             },
             enable: (active: boolean) => {
               pieceEnabled.value = active;
@@ -244,14 +248,14 @@ const Piece = React.memo(
           borderRadius: size,
           zIndex: 0,
           backgroundColor: isGestureActive.value
-            ? 'rgba(0, 0, 0, 0.1)'
+            ? selectedSquareHighlight ?? 'rgba(0, 0, 0, 0.1)'
             : 'transparent',
           transform: [
             { translateX: translation.x - size / 2 },
             { translateY: translation.y - size / 2 },
           ],
         };
-      }, [size]);
+      }, [size, selectedSquareHighlight]);
 
       return (
         <>
diff --git a/node_modules/react-native-chessboard/src/components/suggested-dots/PlaceholderDot.tsx b/node_modules/react-native-chessboard/src/components/suggested-dots/PlaceholderDot.tsx
index ded8edc..873f33f 100644
--- a/node_modules/react-native-chessboard/src/components/suggested-dots/PlaceholderDot.tsx
+++ b/node_modules/react-native-chessboard/src/components/suggested-dots/PlaceholderDot.tsx
@@ -40,7 +40,9 @@ const PlaceholderDot: React.FC<PlaceholderDotProps> = React.memo(
 
     const rPlaceholderStyle = useAnimatedStyle(() => {
       const canBeSelected = isSelectable.value;
-      return { opacity: withTiming(canBeSelected ? 0.15 : 0) };
+      // AJAX patch: era 0.15 (bolinha discreta). O realce agora preenche a
+      // casa inteira, então a opacidade é a do preenchimento amarelo.
+      return { opacity: withTiming(canBeSelected ? 0.62 : 0) };
     }, []);
 
     return (
@@ -54,7 +56,9 @@ const PlaceholderDot: React.FC<PlaceholderDotProps> = React.memo(
           styles.placeholderContainer,
           {
             width: pieceSize,
-            padding: pieceSize / 3.2,
+            // AJAX patch: era `pieceSize / 3.2`, que encolhia o marcador até
+            // virar a bolinha central. Zero = o realce preenche a casa toda.
+            padding: 0,
             transform: [
               { translateX: translation.x },
               { translateY: translation.y },
@@ -64,7 +68,7 @@ const PlaceholderDot: React.FC<PlaceholderDotProps> = React.memo(
       >
         <Animated.View
           style={[
-            { borderRadius: pieceSize },
+            // AJAX patch: sem borderRadius — a casa é quadrada.
             styles.placeholder,
             rPlaceholderStyle,
           ]}
@@ -82,8 +86,10 @@ const styles = StyleSheet.create({
   },
   placeholder: {
     flex: 1,
-    backgroundColor: 'black',
-    opacity: 0.2,
+    // AJAX patch: era 'black' com opacity 0.2 (a bolinha). Amarelo sólido —
+    // mesma cor do realce de último lance, com opacidade MAIOR, porque destino
+    // é a ação disponível agora e o último lance é só contexto.
+    backgroundColor: '#F4EE68',
   },
 });
 
diff --git a/node_modules/react-native-chessboard/src/context/board-operations-context/index.tsx b/node_modules/react-native-chessboard/src/context/board-operations-context/index.tsx
index 2fc9565..a7c4eae 100644
--- a/node_modules/react-native-chessboard/src/context/board-operations-context/index.tsx
+++ b/node_modules/react-native-chessboard/src/context/board-operations-context/index.tsx
@@ -19,7 +19,7 @@ import { useChessboardProps } from '../props-context/hooks';
 
 type BoardOperationsContextType = {
   selectableSquares: Animated.SharedValue<Square[]>;
-  onMove: (from: Square, to: Square) => void;
+  onMove: (from: Square, to: Square, promotionPiece?: PieceType) => void;
   onSelectPiece: (square: Square) => void;
   moveTo: (to: Square) => void;
   isPromoting: (from: Square, to: Square) => boolean;
@@ -148,7 +148,7 @@ const BoardOperationsContextProviderComponent = React.forwardRef<
   );
 
   const onMove = useCallback(
-    (from: Square, to: Square) => {
+    (from: Square, to: Square, promotionPiece?: PieceType) => {
       selectableSquares.value = [];
       selectedSquare.value = null;
       const lastMove = { from, to };
@@ -162,6 +162,13 @@ const BoardOperationsContextProviderComponent = React.forwardRef<
         return;
       }
 
+      // Programmatic moves (engine/opponent) already know the promotion
+      // piece — applying it directly keeps the dialog for human moves only.
+      if (promotionPiece) {
+        moveProgrammatically(from, to, promotionPiece);
+        return;
+      }
+
       pieceRefs?.current?.[to]?.current?.enable(false);
       showPromotionDialog({
         type: chess.turn(),
@@ -187,19 +194,11 @@ const BoardOperationsContextProviderComponent = React.forwardRef<
     (square: Square) => {
       selectedSquare.value = square;
 
-      const validSquares = (chess.moves({
-        square,
-      }) ?? []) as Square[];
-
-      // eslint-disable-next-line no-shadow
-      selectableSquares.value = validSquares.map((square) => {
-        const splittedSquare = square.split('x');
-        if (splittedSquare.length === 0) {
-          return square;
-        }
-
-        return splittedSquare[splittedSquare.length - 1] as Square;
-      });
+      // Verbose moves carry exact destination squares — SAN strings don't
+      // cover castling ("O-O"/"O-O-O" never map to g1/c1).
+      selectableSquares.value = (
+        chess.moves({ square, verbose: true }) ?? []
+      ).map((move) => move.to);
     },
     [chess, selectableSquares, selectedSquare]
   );
diff --git a/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/dialog-piece.tsx b/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/dialog-piece.tsx
index 34c64d0..bed2958 100644
--- a/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/dialog-piece.tsx
+++ b/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/dialog-piece.tsx
@@ -1,94 +1,86 @@
 import type { PieceType } from 'chess.js';
 import React from 'react';
-import { StyleSheet, View } from 'react-native';
-import { Gesture, GestureDetector } from 'react-native-gesture-handler';
-import Animated, {
-  runOnJS,
-  useAnimatedStyle,
-  useSharedValue,
-  withTiming,
-} from 'react-native-reanimated';
+import { Image, Pressable, StyleSheet, View } from 'react-native';
 import type { Player } from '../../../types';
-import { ChessPiece } from '../../../components/piece/visual-piece';
+import { PIECES } from '../../../constants';
 import { useChessboardProps } from '../../../context/props-context/hooks';
 
 type DialogPieceProps = {
-  index: number;
-  width: number;
+  /** Lado da célula em pixels (quadrada). */
+  size: number;
   type: Player;
   piece: PieceType;
   onSelectPiece?: (piece: PieceType) => void;
 };
 
+/**
+ * AJAX patch — uma opção do seletor de promoção.
+ *
+ * Diferenças em relação ao original:
+ *   - célula com borda e canto próprios: as 4 opções leem como 4 botões
+ *     separados, não como um bloco só;
+ *   - a peça é renderizada direto de PIECES, sem passar por `renderPiece`. O app
+ *     usa `renderPiece` para girar as peças 180° quando o jogador é das pretas,
+ *     e dentro do seletor a peça precisa estar sempre de pé;
+ *   - tamanho da peça derivado da célula (o original herdava `pieceSize` do
+ *     tabuleiro, que não tem relação com o tamanho do botão);
+ *   - `Pressable` do próprio React Native no lugar do `GestureDetector` do
+ *     react-native-gesture-handler. O seletor agora vive dentro de um <Modal>
+ *     nativo (ver ./index.tsx), e o gesture-handler não enxerga toques dentro
+ *     de um Modal no Android sem um `GestureHandlerRootView` próprio lá dentro.
+ *     `Pressable` não tem esse problema, e o realce de toque sai do estado
+ *     `pressed` dele — sem reanimated, que também é delicado dentro de Modal.
+ */
 const DialogPiece: React.FC<DialogPieceProps> = React.memo(
-  ({ index, width, type, piece, onSelectPiece }) => {
-    const isTapActive = useSharedValue(false);
+  ({ size, type, piece, onSelectPiece }) => {
     const {
-      colors: { promotionPieceButton },
+      colors: { promotionPieceButton, promotionBorder },
+      promotionLabels,
     } = useChessboardProps();
 
-    const gesture = Gesture.Tap()
-      .onBegin(() => {
-        isTapActive.value = true;
-      })
-      .onTouchesUp(() => {
-        if (onSelectPiece) runOnJS(onSelectPiece)(piece);
-      })
-      .onFinalize(() => {
-        isTapActive.value = false;
-      })
-      .shouldCancelWhenOutside(true)
-      .maxDuration(10000);
-
-    const rStyle = useAnimatedStyle(() => {
-      return {
-        opacity: withTiming(isTapActive.value ? 1 : 0, { duration: 150 }),
-      };
-    }, []);
-
     return (
-      <GestureDetector gesture={gesture}>
-        <Animated.View>
-          <Animated.View
-            style={[
-              {
-                width,
-                position: 'absolute',
-                backgroundColor: promotionPieceButton,
-                aspectRatio: 1,
-                borderTopLeftRadius: index === 0 ? 5 : 0,
-                borderBottomLeftRadius: index === 1 ? 5 : 0,
-                borderTopRightRadius: index === 2 ? 5 : 0,
-                borderBottomRightRadius: index === 3 ? 5 : 0,
-              } as const,
-              rStyle,
-            ]}
-          />
-          <View
-            style={[
-              {
-                width,
-                borderLeftWidth: index === 3 || index === 2 ? 1 : 0,
-                borderTopWidth: index % 2 !== 0 ? 1 : 0,
-              } as const,
-              styles.pieceContainer,
-            ]}
-          >
-            <ChessPiece id={`${type}${piece}`} />
-          </View>
-        </Animated.View>
-      </GestureDetector>
+      <Pressable
+        onPress={() => onSelectPiece?.(piece)}
+        style={[
+          styles.cell,
+          { width: size, height: size, borderColor: promotionBorder },
+        ]}
+        accessibilityRole="button"
+        accessibilityLabel={promotionLabels?.[piece] ?? piece.toUpperCase()}
+      >
+        {({ pressed }) => (
+          <>
+            {/* Realce de toque por cima do fundo opaco do card. */}
+            {pressed && (
+              <View
+                style={[
+                  StyleSheet.absoluteFill,
+                  styles.pressed,
+                  { backgroundColor: promotionPieceButton },
+                ]}
+              />
+            )}
+            <Image
+              source={PIECES[`${type}${piece}`]}
+              style={{ width: size * 0.82, height: size * 0.82 }}
+            />
+          </>
+        )}
+      </Pressable>
     );
   }
 );
 
 const styles = StyleSheet.create({
-  pieceContainer: {
-    aspectRatio: 1,
+  cell: {
     justifyContent: 'center',
     alignItems: 'center',
-    borderBottomWidth: 0,
-    borderColor: 'rgba(0,0,0,0.2)',
+    borderRadius: 10,
+    borderWidth: 1,
+    overflow: 'hidden',
+  },
+  pressed: {
+    borderRadius: 10,
   },
 });
 
diff --git a/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/index.tsx b/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/index.tsx
index 481593d..1303d9d 100644
--- a/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/index.tsx
+++ b/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/index.tsx
@@ -1,61 +1,135 @@
 import { useChessboardProps } from '../../../context/props-context/hooks';
 import React from 'react';
 import type { PieceType } from 'chess.js';
-import Animated, { FadeIn, FadeOut } from 'react-native-reanimated';
 
-import { StyleSheet } from 'react-native';
+import { Modal, StyleSheet, View } from 'react-native';
 import type { BoardPromotionContextState } from '..';
 import { DialogPiece } from './dialog-piece';
 
-const PROMOTION_PIECES: PieceType[] = ['q', 'r', 'n', 'b'];
+// AJAX patch: ordem Q/R/B/N (dama, torre, bispo, cavalo) — a ordem de
+// frequência real da promoção, e a mesma dos apps de referência.
+const PROMOTION_PIECES: PieceType[] = ['q', 'r', 'b', 'n'];
 
+/**
+ * AJAX patch — seletor de promoção, em <Modal> NATIVO.
+ *
+ * Histórico, porque isso já foi "corrigido" duas vezes sem resolver:
+ *
+ *   1. O original da lib era um painel 2×2 `rgba(256,256,256,0.85)`
+ *      (semitransparente) em `position: absolute` SEM top/left: caía no canto
+ *      do contêiner do tabuleiro, com as peças reais aparecendo por baixo.
+ *   2. A 1ª correção trocou isso por backdrop + card opaco, ainda em
+ *      `position: absolute` + `zIndex: 10`, DENTRO da árvore de views do
+ *      tabuleiro. Os números ficavam certos (verificável renderizando a
+ *      árvore), mas no Android continuava quebrado no device.
+ *
+ * Por que `position: absolute` + `zIndex` não basta no Android: o RN não
+ * implementa stacking context de CSS. `zIndex` só reordena o desenho entre
+ * IRMÃOS diretos de um mesmo ViewGroup, e este diálogo é renderizado pelo
+ * BoardPromotionContextProvider como irmão ANTERIOR ao tabuleiro — depende
+ * inteiramente do `zIndex: 10` vencer. As peças, dentro do tabuleiro, também
+ * usam `zIndex` (10 parado, 100 durante o arraste, ver components/piece).
+ * Empate de valores e ordem de irmãos passam a decidir o empilhamento, e o
+ * resultado diverge entre Android e iOS.
+ *
+ * A correção estrutural é não disputar: um <Modal> nativo é uma JANELA
+ * separada da árvore de views do app. Não compete com `zIndex` nem com
+ * `elevation` de nenhum outro elemento, por definição — elimina a classe
+ * inteira do problema em vez de ajustar números de novo.
+ *
+ * Consequências deliberadas dessa mudança:
+ *   - NÃO existe mais contra-rotação por `flipped`. Ela só existia porque o
+ *     diálogo ficava dentro do contêiner do tabuleiro, que o app gira 180°
+ *     para jogar de pretas. Fora dessa árvore, contra-rotacionar deixaria o
+ *     seletor de cabeça para baixo.
+ *   - O backdrop cobre a TELA inteira, não só o tabuleiro (padrão de mercado,
+ *     e o que um <Modal> naturalmente faz).
+ *   - Sem `react-native-reanimated` e sem `react-native-gesture-handler` aqui
+ *     dentro: ambos exigem cuidado extra dentro de um Modal no Android (o
+ *     gesture-handler precisaria de um `GestureHandlerRootView` próprio, sem o
+ *     qual os toques simplesmente não chegam). A animação fica por conta do
+ *     `animationType` do próprio Modal, e o toque por conta do `Pressable`
+ *     (ver dialog-piece). Menos peças móveis, menos superfície para regressão.
+ */
 const PromotionDialog: React.FC<Required<BoardPromotionContextState>> =
   React.memo(({ type, onSelect }) => {
-    const { boardSize } = useChessboardProps();
+    const {
+      boardSize,
+      colors: { promotionBackdrop, promotionSurface, promotionBorder },
+    } = useChessboardProps();
+
+    // Alvo de toque confortável mesmo em telas pequenas: boardSize/4 fica em
+    // ~80–100dp, bem acima do mínimo de 44dp. O piso de 56 protege o caso em
+    // que o diálogo abrisse antes de o tabuleiro ter sido medido (boardSize 0),
+    // que renderizaria um card de tamanho zero — invisível.
+    const cellSize = Math.max(Math.floor(boardSize / 4), 56);
+    // Largura fechada na conta da grade 2×2 (2 células + gap + padding), para o
+    // wrap ser determinístico em qualquer tamanho de tabuleiro.
+    const cardWidth = cellSize * 2 + CARD_GAP + CARD_PADDING * 2;
 
     return (
-      <Animated.View
-        entering={FadeIn}
-        exiting={FadeOut}
-        style={[
-          {
-            width: boardSize / 3,
-          },
-          styles.container,
-        ]}
+      <Modal
+        visible
+        transparent
+        animationType="fade"
+        // Cobre também a área da status bar no Android; sem isso o backdrop
+        // para antes do topo e deixa uma faixa clara.
+        statusBarTranslucent
+        // Obrigatório no Android. A promoção não é cancelável — o jogador
+        // precisa escolher uma peça — então o botão voltar é engolido de
+        // propósito, em vez de derrubar a tela no meio do lance.
+        onRequestClose={() => {}}
       >
-        {PROMOTION_PIECES.map((piece, i) => {
-          return (
-            <DialogPiece
-              key={i}
-              width={boardSize / 6}
-              index={i}
-              piece={piece}
-              type={type}
-              onSelectPiece={onSelect}
-            />
-          );
-        })}
-      </Animated.View>
+        <View style={[styles.backdrop, { backgroundColor: promotionBackdrop }]}>
+          <View
+            style={[
+              styles.card,
+              {
+                width: cardWidth,
+                backgroundColor: promotionSurface,
+                borderColor: promotionBorder,
+              },
+            ]}
+          >
+            {PROMOTION_PIECES.map((piece) => (
+              <DialogPiece
+                key={piece}
+                size={cellSize}
+                piece={piece}
+                type={type}
+                onSelectPiece={onSelect}
+              />
+            ))}
+          </View>
+        </View>
+      </Modal>
     );
   });
 
+const CARD_GAP = 8;
+const CARD_PADDING = 12;
+
 const styles = StyleSheet.create({
-  container: {
-    position: 'absolute',
-    aspectRatio: 1,
-    backgroundColor: 'rgba(256,256,256,0.85)',
-    borderRadius: 5,
-    zIndex: 10,
+  backdrop: {
+    flex: 1,
     justifyContent: 'center',
     alignItems: 'center',
-    shadowColor: 'black',
-    shadowOpacity: 0.2,
-    shadowOffset: {
-      height: 5,
-      width: 0,
-    },
+  },
+  // 2×2: quatro células de boardSize/4 não caberiam em uma linha só dentro do
+  // tabuleiro, e a grade dá alvos de toque maiores.
+  card: {
+    flexDirection: 'row',
     flexWrap: 'wrap',
+    justifyContent: 'center',
+    gap: CARD_GAP,
+    padding: CARD_PADDING,
+    borderRadius: 16,
+    borderWidth: 1,
+    shadowColor: 'black',
+    shadowOpacity: 0.3,
+    shadowRadius: 12,
+    shadowOffset: { height: 6, width: 0 },
+    elevation: 8,
   },
 });
 
diff --git a/node_modules/react-native-chessboard/src/context/board-refs-context/index.tsx b/node_modules/react-native-chessboard/src/context/board-refs-context/index.tsx
index 3966cd9..67a71ef 100644
--- a/node_modules/react-native-chessboard/src/context/board-refs-context/index.tsx
+++ b/node_modules/react-native-chessboard/src/context/board-refs-context/index.tsx
@@ -1,4 +1,4 @@
-import type { Move, Square } from 'chess.js';
+import type { Move, PieceType, Square } from 'chess.js';
 import React, {
   createContext,
   useCallback,
@@ -29,6 +29,7 @@ export type ChessboardRef = {
   move: (_: {
     from: Square;
     to: Square;
+    promotion?: PieceType;
   }) => Promise<Move | undefined> | undefined;
   highlight: (_: { square: Square; color?: string }) => void;
   resetAllHighlightedSquares: () => void;
@@ -75,8 +76,8 @@ const BoardRefsContextProviderComponent = React.forwardRef<
   useImperativeHandle(
     ref,
     () => ({
-      move: ({ from, to }) => {
-        return pieceRefs?.current?.[from].current?.moveTo?.(to);
+      move: ({ from, to, promotion }) => {
+        return pieceRefs?.current?.[from].current?.moveTo?.(to, promotion);
       },
       highlight: ({ square, color }) => {
         squareRefs.current?.[square].current.highlight({
diff --git a/node_modules/react-native-chessboard/src/context/props-context/index.tsx b/node_modules/react-native-chessboard/src/context/props-context/index.tsx
index 448aa9c..7300d7d 100644
--- a/node_modules/react-native-chessboard/src/context/props-context/index.tsx
+++ b/node_modules/react-native-chessboard/src/context/props-context/index.tsx
@@ -15,7 +15,18 @@ type ChessboardColorsType = {
   black: string;
   lastMoveHighlight?: string;
   checkmateHighlight?: string;
+  /** Realce de toque de uma opção do seletor de promoção. */
   promotionPieceButton?: string;
+  // AJAX patch: cor do brilho sob a peça na casa selecionada/arrastada.
+  // Permite temas de tabuleiro (PR E) usarem o Dourado AJAX na seleção.
+  selectedSquareHighlight?: string;
+  // AJAX patch: seletor de promoção opaco com backdrop (ver dialog/index.tsx).
+  /** Escurecimento sobre o tabuleiro enquanto o seletor está aberto. */
+  promotionBackdrop?: string;
+  /** Fundo do card do seletor. Precisa ser OPACO. */
+  promotionSurface?: string;
+  /** Borda do card e das 4 opções. */
+  promotionBorder?: string;
 };
 
 type ChessboardDurationsType = {
@@ -62,6 +73,23 @@ type ChessboardProps = {
    * Useful if you want to customise the default durations used in the chessboard (in milliseconds).
    */
   durations?: ChessboardDurationsType;
+  /**
+   * AJAX patch: indica que o app girou o contêiner do tabuleiro em 180°
+   * (jogador das pretas). Não afeta o tabuleiro em si — a rotação continua
+   * sendo do app.
+   *
+   * NÃO É MAIS CONSUMIDO desde que o seletor de promoção virou um <Modal>
+   * nativo: fora da árvore do tabuleiro, não há rotação do contêiner para
+   * compensar. Mantido no tipo porque as três telas ainda o passam, e para
+   * não quebrar a assinatura pública do componente.
+   */
+  flipped?: boolean;
+  /**
+   * AJAX patch: rótulos acessíveis das opções de promoção, por peça
+   * (ex.: `{ q: 'Promover para dama' }`). Sem eles o leitor de tela anuncia
+   * apenas a letra da peça.
+   */
+  promotionLabels?: Partial<Record<'q' | 'r' | 'b' | 'n', string>>;
 };
 
 type ChessboardContextType = ChessboardProps &
@@ -85,7 +113,14 @@ const defaultChessboardProps: ChessboardContextType = {
     white: '#D9FDF8',
     lastMoveHighlight: 'rgba(255,255,0, 0.5)',
     checkmateHighlight: '#E84855',
-    promotionPieceButton: '#FF9B71',
+    // AJAX patch: o default da lib era '#FF9B71' — laranja, proibido na marca
+    // (RF-VISUAL-01). O default agora é neutro para que uma tela que esqueça de
+    // passar `colors` nunca reintroduza laranja.
+    promotionPieceButton: 'rgba(0, 0, 0, 0.12)',
+    selectedSquareHighlight: 'rgba(0, 0, 0, 0.1)',
+    promotionBackdrop: 'rgba(0, 0, 0, 0.62)',
+    promotionSurface: '#FFFFFF',
+    promotionBorder: 'rgba(0, 0, 0, 0.18)',
   },
   durations: {
     move: 150,
